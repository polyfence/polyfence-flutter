<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- ACCESS_BACKGROUND_LOCATION buys exactly one thing: OS wake fences
         (osGeofenceWakeEnabled). Tracking runs as a foreground service typed
         "location", which holds location access without it. Declaring it is
         what puts an app into Google Play's manual background-location review,
         so it stays out of the minimum set and is uncommented alongside
         kOsGeofenceWakeEnabled in lib/main.dart. RECEIVE_BOOT_COMPLETED goes
         with it — Play Services drops every registered geofence on reboot.
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Activity recognition powers the SmartGPS intelligent strategy
         (still / walking / running / cycling / driving). Optional at
         runtime — the example continues without it if denied. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />

    <application
        android:label="Polyfence Flutter Example"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- LocationTracker service for background geofencing. The class
             ships in polyfence-core, not in the Flutter plugin's own
             namespace. foregroundServiceType="location" is what grants the
             service its location access — required from API 29 and hard
             enforced from API 34, where startForeground() throws without it. -->
        <service
            android:name="io.polyfence.core.LocationTracker"
            android:foregroundServiceType="location"
            android:exported="false"
            android:enabled="true" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
